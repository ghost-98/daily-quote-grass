# Contributing

## 브랜치 컨벤션

```text
main
feat/<short-topic>
fix/<short-topic>
docs/<short-topic>
chore/<short-topic>
test/<short-topic>
refactor/<short-topic>
codex/<type>/<short-topic>
```

## 커밋 메시지 컨벤션

Conventional Commits의 타입과 스코프는 영어를 사용하고, 설명은 한글 현재형으로 작성합니다.

```text
<type>(<scope>): <한글 현재형 설명>
```

예시:

```text
chore(project): 프로젝트 기본 구조를 만든다
feat(api): 오늘의 명언 API 클라이언트를 추가한다
feat(quote): 일일 명언 기록 기능을 추가한다
feat(ui): 명언 기록 대시보드를 추가한다
test(quote): 명언 기록 로직을 검증한다
ci(quote): 일일 명언 기록 워크플로를 추가한다
chore(quote): 오늘의 명언을 기록한다
```
